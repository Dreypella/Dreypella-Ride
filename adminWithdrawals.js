/* ======================================================== DREYPELLA RIDE SECURE ADMIN WITHDRAWAL FUNCTIONS ======================================================== Purpose: - Approve customer/partner/vendor/ambassador withdrawals - Decline withdrawals - Protect wallet balances - Prevent double processing - Create wallet transaction records - Create admin audit records IMPORTANT: This file runs on Firebase Cloud Functions. It must NEVER be exposed directly to customers. ======================================================== */ const functions = require("firebase-functions"); const admin = require("firebase-admin"); const db = admin.firestore(); /* ===================================================== ADMIN AUTHORIZATION ===================================================== */ async function requireAdmin(context) { if (!context.auth) { throw new functions.https.HttpsError( "unauthenticated", "You must be logged in." ); } const uid = context.auth.uid; const adminRef = db .collection("admins") .doc(uid); const adminSnap = await adminRef.get(); if (!adminSnap.exists) { throw new functions.https.HttpsError( "permission-denied", "Admin access is required." ); } const adminData = adminSnap.data(); if ( adminData.active === false ) { throw new functions.https.HttpsError( "permission-denied", "This admin account is disabled." ); } return { uid, ...adminData }; } /* ===================================================== VALIDATE WITHDRAWAL ===================================================== */ function validateWithdrawalId( withdrawalId ) { if ( typeof withdrawalId !== "string" || !withdrawalId.trim() ) { throw new functions.https.HttpsError( "invalid-argument", "A valid withdrawal ID is required." ); } } /* ===================================================== APPROVE WITHDRAWAL ===================================================== */ exports.approveWithdrawal = functions.https.onCall( async (data, context) => { const adminUser = await requireAdmin( context ); const withdrawalId = data.withdrawalId; const note = typeof data.note === "string" ? data.note.trim() : ""; validateWithdrawalId( withdrawalId ); const withdrawalRef = db .collection("withdrawals") .doc(withdrawalId); try { const result = await db.runTransaction( async (transaction) => { const withdrawalSnap = await transaction.get( withdrawalRef ); if ( !withdrawalSnap.exists ) { throw new functions.https.HttpsError( "not-found", "Withdrawal does not exist." ); } const withdrawal = withdrawalSnap.data(); /* * Prevent double processing. */ if ( withdrawal.status !== "PENDING" ) { throw new functions.https.HttpsError( "failed-precondition", `Withdrawal is already ${withdrawal.status}.` ); } const amount = Number( withdrawal.amount ); if ( !Number.isFinite(amount) || amount <= 0 ) { throw new functions.https.HttpsError( "invalid-argument", "Invalid withdrawal amount." ); } const userId = withdrawal.userId; if ( !userId ) { throw new functions.https.HttpsError( "failed-precondition", "Withdrawal has no user ID." ); } /* * Locate the user's wallet. */ const walletRef = db .collection("wallets") .doc(userId); const walletSnap = await transaction.get( walletRef ); if ( !walletSnap.exists ) { throw new functions.https.HttpsError( "failed-precondition", "User wallet does not exist." ); } const wallet = walletSnap.data(); const availableBalance = Number( wallet.availableBalance || 0 ); /* * If the withdrawal amount * was not already reserved, * protect against insufficient funds. */ if ( availableBalance >= amount ) { transaction.update( walletRef, { availableBalance: admin.firestore.FieldValue.increment( -amount ), updatedAt: admin.firestore.FieldValue.serverTimestamp() } ); } else { /* * Some withdrawal systems * reserve the funds when the * request is created. * * If funds are already reserved, * the available balance may be * lower than the withdrawal. */ const reservedBalance = Number( wallet.reservedBalance || 0 ); if ( reservedBalance < amount ) { throw new functions.https.HttpsError( "failed-precondition", "Insufficient wallet funds." ); } transaction.update( walletRef, { reservedBalance: admin.firestore.FieldValue.increment( -amount ), updatedAt: admin.firestore.FieldValue.serverTimestamp() } ); } /* * Mark withdrawal approved. */ transaction.update( withdrawalRef, { status: "APPROVED", approvedBy: adminUser.uid, approvedAt: admin.firestore.FieldValue.serverTimestamp(), adminNote: note, updatedAt: admin.firestore.FieldValue.serverTimestamp() } ); /* * Create transaction record. */ const transactionRef = db .collection( "walletTransactions" ) .doc(); transaction.set( transactionRef, { transactionId: transactionRef.id, userId: userId, walletId: userId, type: "WITHDRAWAL", direction: "DEBIT", amount: amount, status: "APPROVED", reference: withdrawal.withdrawalReference || withdrawalId, withdrawalId: withdrawalId, description: "Wallet withdrawal approved by admin.", performedBy: adminUser.uid, createdAt: admin.firestore.FieldValue.serverTimestamp() } ); /* * Create audit record. */ const auditRef = db .collection( "adminAuditLogs" ) .doc(); transaction.set( auditRef, { action: "APPROVE_WITHDRAWAL", adminId: adminUser.uid, withdrawalId: withdrawalId, userId: userId, amount: amount, note: note, createdAt: admin.firestore.FieldValue.serverTimestamp() } ); return { withdrawalId, amount, status: "APPROVED" }; } ); return { success: true, ...result }; } catch(error) { console.error( "Approve withdrawal error:", error ); if ( error instanceof functions.https.HttpsError ) { throw error; } throw new functions.https.HttpsError( "internal", "Unable to approve withdrawal." ); } } ); /* ===================================================== DECLINE WITHDRAWAL ===================================================== */ exports.declineWithdrawal = functions.https.onCall( async (data, context) => { const adminUser = await requireAdmin( context ); const withdrawalId = data.withdrawalId; const note = typeof data.note === "string" ? data.note.trim() : ""; validateWithdrawalId( withdrawalId ); if ( !note ) { throw new functions.https.HttpsError( "invalid-argument", "Please provide a reason for declining the withdrawal." ); } const withdrawalRef = db .collection("withdrawals") .doc(withdrawalId); try { const result = await db.runTransaction( async (transaction) => { const withdrawalSnap = await transaction.get( withdrawalRef ); if ( !withdrawalSnap.exists ) { throw new functions.https.HttpsError( "not-found", "Withdrawal does not exist." ); } const withdrawal = withdrawalSnap.data(); if ( withdrawal.status !== "PENDING" ) { throw new functions.https.HttpsError( "failed-precondition", `Withdrawal is already ${withdrawal.status}.` ); } const amount = Number( withdrawal.amount ); const userId = withdrawal.userId; if ( !userId || !Number.isFinite(amount) || amount <= 0 ) { throw new functions.https.HttpsError( "failed-precondition", "Invalid withdrawal record." ); } const walletRef = db .collection("wallets") .doc(userId); const walletSnap = await transaction.get( walletRef ); if ( !walletSnap.exists ) { throw new functions.https.HttpsError( "failed-precondition", "User wallet does not exist." ); } const wallet = walletSnap.data(); /* * If funds were reserved when * the withdrawal was requested, * return them to available balance. */ const reservedBalance = Number( wallet.reservedBalance || 0 ); if ( reservedBalance >= amount ) { transaction.update( walletRef, { reservedBalance: admin.firestore.FieldValue.increment( -amount ), availableBalance: admin.firestore.FieldValue.increment( amount ), updatedAt: admin.firestore.FieldValue.serverTimestamp() } ); } /* * Mark withdrawal declined. */ transaction.update( withdrawalRef, { status: "DECLINED", declinedBy: adminUser.uid, declinedAt: admin.firestore.FieldValue.serverTimestamp(), adminNote: note, updatedAt: admin.firestore.FieldValue.serverTimestamp() } ); /* * Create reversal/refund transaction * only when funds had been reserved. */ if ( reservedBalance >= amount ) { const transactionRef = db .collection( "walletTransactions" ) .doc(); transaction.set( transactionRef, { transactionId: transactionRef.id, userId: userId, walletId: userId, type: "WITHDRAWAL_REVERSAL", direction: "CREDIT", amount: amount, status: "SUCCESS", reference: withdrawal.withdrawalReference || withdrawalId, withdrawalId: withdrawalId, description: "Declined withdrawal funds returned to wallet.", performedBy: adminUser.uid, createdAt: admin.firestore.FieldValue.serverTimestamp() } ); } /* * Admin audit trail. */ const auditRef = db .collection( "adminAuditLogs" ) .doc(); transaction.set( auditRef, { action: "DECLINE_WITHDRAWAL", adminId: adminUser.uid, withdrawalId: withdrawalId, userId: userId, amount: amount, note: note, createdAt: admin.firestore.FieldValue.serverTimestamp() } ); return { withdrawalId, amount, status: "DECLINED" }; } ); return { success: true, ...result }; } catch(error) { console.error( "Decline withdrawal error:", error ); if ( error instanceof functions.https.HttpsError ) { throw error; } throw new functions.https.HttpsError( "internal", "Unable to decline withdrawal." ); } } ); 